# AGENTS.md

このファイルは、別のPCや新しいCodexタスクでも、このプロジェクトの開発条件を引き継ぐための作業指示です。仕様の詳細と検証履歴は `README.md` を参照してください。

## 対話・報告方針

- 日本語で、直接的かつ丁寧に回答する。
- ユーザーはITエンジニアである。簡潔さよりも技術的な正確さを優先する。
- 確認できた事実と、観察・推論・提案を明確に区別する。未確認事項を断定しない。
- 変更内容、変更理由、実行した検証、その結果、未検証事項を報告する。
- 過去の検証結果を、現在のコードや別PCで実行済みの結果として扱わない。シミュレータ・エミュレータでの検証と実機検証を区別する。

## プロジェクトの前提

- プロジェクト名は `light_register`、アプリ名は「出店レジ」。iOS・Android向けのFlutterアプリである。
- インストール後の初回起動からオフラインで使用できることを維持する。商品登録・会計・売上表示にネットワーク接続やログインを必須にしない。
- 商品登録・編集、注文作成・数量変更、現金会計・釣り銭表示、日別・商品別売上表示、売上記録消去、データ全消去を同じアプリ内で扱う。
- データは端末内SQLiteに保存する。商品・未会計注文・確定済み売上の永続化を維持する。
- 「TTOの使い勝手」という当初の表現に、特定製品や再現対象の指定はない。
- 現在の対象はiOS・Androidであり、Web版は実装していない。仕様変更はその時点のユーザーの依頼に従う。

## 変更時に維持する動作

- 金額は税込の日本円を整数で扱う。金額・数量の上限は `lib/domain/models.dart` の検証と整合させる。
- 売上保存と未会計注文のクリアは、同一のSQLiteトランザクションで確定する。失敗時のロールバックと、同じ注文の再試行による二重計上の防止を維持する。
- 会計明細には会計時点の商品情報を保持する。商品編集・販売終了で過去の売上明細を変更しない。
- 未会計注文では、追加時点の商品名・単価・カテゴリが異なる明細を分けて保持し、数量変更・再接続・会計後も各明細の商品情報を維持する。
- 検索欄の表示と絞り込み条件を一致させる。販売中の商品がなくなった場合は検索・カテゴリの選択をリセットし、その後登録した商品を表示する。
- 売上記録消去は、表示期間にかかわらず全期間の売上・会計明細を消去し、登録商品・販売終了商品・未会計注文を残す。
- データ全消去は、商品・未会計注文・全期間の売上を消去し、新しいIDの空の注文で再開する。消去の確認・キャンセル、失敗時のデータ保持、成功後の画面更新を維持する。
- スマートフォン・タブレットの縦横画面で、商品選択・数量変更・会計を操作できることを確認する。固定領域が明細の高さを使い切らないようにし、高さが足りない場合はスクロールで各操作に到達できるようにする。商品管理では横画面でのキーボード表示中もレイアウトを保ち、編集・保存できることを確認する。

## 作業開始時の確認

- `git status --short --branch`、リモート、直近のコミットを確認し、現在の作業状態を把握する。
- `README.md`、`pubspec.yaml`、変更対象の実装と関連テストを読む。引き継ぎ時の説明と現在のコードが異なる場合は、差異を確認してから判断する。
- 既存のユーザー変更を保持し、依頼の範囲に沿って作業する。
- ファイル検索・文字列検索は、利用可能なら `rg --files`・`rg` を使用する。

## 主な構成

- `lib/domain/models.dart`：モデル、金額・数量検証、売上集計。
- `lib/data/register_database.dart`：SQLiteスキーマ、保存、注文の版管理、会計・消去トランザクション。
- `lib/register_controller.dart`：画面状態と保存処理の排他制御。
- `lib/ui/`：レジ、商品管理、会計、売上、データ管理の画面。
- `test/`：SQLiteを用いた保存・会計・消去の検証、画面操作・横画面の回帰テスト。
- `integration_test/register_test.dart`：ネイティブSQLite、画面回転・会計、再接続による復元、データ消去の統合テスト。

## 開発環境と検証

- 現在の開発環境はFlutter 3.47.3 / Dart 3.13.3。環境を再現する場合はこのバージョンを基準とし、更新が必要な場合は互換性を確認する。最低対応OSはiOS 15.0・Android API 24。以前のバージョンでの検証は `README.md` の履歴と区別する。
- 別PCでは `flutter --version` と `flutter doctor` で環境を確認し、`flutter pub get` で依存関係を取得する。理由なく依存関係やロックファイルを更新しない。
- `.fvmrc` はFlutter 3.47.3を指定している。FVMを使う場合はプロジェクト直下でSDKを準備し、下記コマンドの `flutter`・`dart` を `fvm flutter`・`fvm dart` に読み替える。Android統合テスト用スクリプトは子プロセスにもSDKを適用するため `fvm exec dart run tool/test_android_integration.dart DEVICE_ID` を使う。ホーム階層や他プロジェクトのSDK設定を変更しない。
- iOSのネイティブ依存関係はSwift Package Managerで管理する。現在の構成では `Podfile`・`Podfile.lock`・Pods参照は不要で、`pod install` は実行しない。新しいプラグインを追加する場合はiOS実装のSwift Package Manager対応を確認し、生成される `ios/Flutter/ephemeral/` はGitに追加しない。
- AndroidビルドではGradleとJDKの互換性を確認する。現在はGradle 9.7.1 / AGP 9.4.0 / Kotlin 2.4.20 / NDK 30.0.16248370を使用する。ビルド用JDKはWindowsで26.0.2.1、MacでAndroid Studio付属の21.0.8を使用する。KotlinはAGP組み込み機能を使用し、Flutterとの互換性のため `android.newDsl=false` を維持する。`flutter doctor -v` で実際に選択されたJavaを確認する。設定方法は `README.md` の「WindowsでAndroidを実行する場合」を参照する。
- 個人の絶対パス、Flutter SDKの設置場所、シミュレータIDを固定しない。実行対象は `flutter devices` で確認する。iOSのビルド・実行にはMacとXcodeが必要である。
- Dartコードを変更した場合は変更ファイルを整形し、静的解析と影響範囲に応じたテストを実行する。バグ修正では、可能な範囲で実際の再現シナリオを回帰テストにする。
- 画面のテストでは、表示の存在確認だけでなく、タップ・スクロール・数量・会計結果など、変更対象の操作と結果を確認する。横画面の既存回帰ケースは844×390・667×375、画面回転、安全領域、文字1.3倍を含む。
- ネイティブ依存や端末固有の挙動を変更した場合は、関連するOSの統合テスト・ビルドで確認する。実行できない検証は、その理由とともに未検証として報告する。
- ドキュメントのみの変更では、内容・参照先・差分を確認する。Flutterのテストやビルドを再実行する必要はない。
- 検証は変更に見合った範囲で行う。成功後は、新たな変更・失敗・未解決の懸念がない限り、同じ検証を繰り返さない。

主要なコマンドは以下のとおり。`DEVICE_ID` は実際の対象端末IDに置き換える。

```sh
flutter analyze
flutter test
flutter test test/landscape_test.dart
flutter test integration_test/register_test.dart -d DEVICE_ID
dart run tool/test_android_integration.dart DEVICE_ID
flutter build apk --debug --target lib/main.dart
flutter build ios --simulator --debug --no-codesign --target lib/main.dart
git diff --check
```

- Flutterのネイティブ統合テスト・ビルドは並行実行せず、順番に実行する。
- Android 16以降のタブレットでは `SystemChrome.setPreferredOrientations` が無視されるため、ADBから回転する `tool/test_android_integration.dart` を使用する。通常の統合テストコマンドと両方実行する必要はない。スクリプトは回転設定を終了時に復元する。
- 統合テスト後に通常起動用のアプリを渡す場合は、`--target lib/main.dart` を指定して再ビルドする。統合テストが生成したテスト用アプリを通常版として渡さない。
- テストには独立した一時DBを使用し、利用者の実データを消去しない。検証のために変更したエミュレータの通信設定などは、完了後に元へ戻す。

## Gitと引き継ぎ

- コミット・pushはユーザーの依頼に基づいて行う。依頼済みの操作について、不要な再確認を求めない。
- コミット対象の差分を確認し、ステージ後に `git diff --cached --check` を実行する。ビルド成果物、端末内DB、署名鍵、認証情報、端末固有の設定を追加しない。
- push先とブランチを確認し、既存の履歴を上書きしない。push後はリモートのコミットIDとローカルHEADの一致を確認し、ブランチ・コミットID・未コミット変更の有無を報告する。
- 仕様や検証手順を変更した場合は、`README.md` と関連する指示を更新する。継続作業の経緯を残す場合は、確認済み事項・未解決事項・次の作業を区別して記録する。
- このファイルは作業指示を引き継ぐものであり、会話履歴やCodexのモデル・権限・プラグイン設定を移行するものではない。別PCで必要な設定は個別に確認する。
